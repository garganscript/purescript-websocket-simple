{
  "private": true,
  "scripts": {
    "build": "pulp build -- --censor-lib",
    "example": "pulp build -I example/src/ -- --censor-lib"
  },
  "dependencies": {
    "ws": "^8.17.0"
  },
  "devDependencies": {
    "purescript": "^0.15.5"
  }
}
